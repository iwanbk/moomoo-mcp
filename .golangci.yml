version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - revive
  disable:
    - errcheck

  settings:
    revive:
      rules:
        - name: exported
          disabled: true
        - name: package-comments
          disabled: true
